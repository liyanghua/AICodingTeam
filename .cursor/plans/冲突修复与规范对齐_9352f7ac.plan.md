---
name: 冲突修复与规范对齐
overview: 将当前 plan 与新规范对齐：修复 4 条冲突（PatchSet 形态升级、action 白名单扩容至 6 个、Provider text fallback、Preview env 注入），废弃自创 replace_in_block 改用规范 replace_text。本轮 Phase 1 必须补齐最小 app_patch_targets、可执行 fallback、patch 后 allowlist 验证、PatchSet apply 事务回滚、图片类生成默认模型；AppPreviewContext 完整派生、AdjustmentEvent、rollback 闭环和 promote 候选记录延后 Phase 2。
todos:
  - id: c1_patchset_backend
    content: C1 后端：patch_app_generation_run 支持 patches:[] 批量 + preserve_capabilities/verification/problem_source 元数据；向后兼容单文件自动包成 patches；dry-run 全量校验；apply 使用备份/临时写入保证失败回滚；patch 后执行 allowlist 验证
    status: pending
  - id: c1_patchset_frontend
    content: C1 前端：handlePatchAppAction 同时支持单文件（action.target_path）和批量（action.patches）；diff modal 渲染合并 diff；body 透传 4 字段
    status: pending
  - id: c2_replace_text
    content: C2：_apply_edit 新增 replace_text 分支（精确匹配 old_content 一次替换，0 次或 >1 次抛错）；移除 replace_in_block 分支
    status: pending
  - id: c3_allowlist_six
    content: C3：PI_ALLOWED_ACTION_TYPES 补 6 个 action；前端 handleAgentAction 对后 4 个 stub appendAgentLog 占位
    status: pending
  - id: c4_preview_env_inject
    content: C4：preview.py 新增 _inject_preview_env(repo_root) 读 .env 白名单 13 字段；start_preview Popen 前合并 env；dashboard.py preview/start API 兼容 inject_env/sync_env
    status: pending
  - id: g2_patchset_schema_prompt
    content: G2：_PI_TRAILING_ACTIONS_PROTOCOL 重写为 4 段（协议规则 + 路由表 + PatchSet schema + 硬性约束）；old_content 必须从 app_patch_targets 锚点复制
    status: pending
  - id: g3_intent_routing
    content: G3：_resolve_intent app_preview focus + 修复关键词 → patch_app；_baseline_actions 补 patch_app 占位
    status: pending
  - id: g4_app_files_inject
    content: G4：_agent_prompt_context 在 focus.card==app_preview 时注入最小 app_patch_targets（path/size/hash/summary/agent_edit_anchors），不默认注入文件正文；dashboard.py 透传 run_dir
    status: pending
  - id: fallback_draft
    content: Provider text fallback：_parse_trailing_actions 后，resolved_intent=patch_app + 无结构化 action + 含修复关键字 → 能定位目标时生成真实 PatchSet；不能定位时返回 diagnose_app_bug/clarify，不允许空 patches 的 patch_app
    status: pending
  - id: c5_generation_default_model
    content: C5：图片类 PRD 的 app_generation scaffold/prompt/contract 默认 OpenRouter 模型对齐为 openai/gpt-5.4-image-2；禁止继续默认写死 gpt-image-1
    status: pending
  - id: tests_bridge
    content: test_agent_bridge.py：新增 PatchSet schema / 6 action 白名单 / 可执行 fallback / 保留原 intent routing + app_patch_targets 用例
    status: pending
  - id: tests_dashboard
    content: test_dashboard.py：新增 replace_text 3 用例（成功/not_found/ambiguous）+ PatchSet dry_run/apply 事务 + 单文件向后兼容
    status: pending
  - id: tests_preview_env
    content: test_preview.py（或 test_dashboard）：inject_preview_env 白名单 / 过滤非白名单 / secret redaction
    status: pending
  - id: regression_e2e
    content: 跑 7 模块 unittest 全绿；E2E 验证 patch_app + diff modal + 两阶段重启 + /api/health + secret redaction；补 docs/round6_phase1_复盘.md
    status: pending
isProject: false
---

## 冲突修复与规范对齐

当前 plan（G1-G6）与新规范存在 4 条明确冲突：

1. **PatchSet 形态**：plan 用单文件 `{target_path, edit_kind, anchor, replacements:[{old,new}]}`；规范要求 `patches:[]` 数组、`preserve_capabilities`、`verification`、`problem_source`
2. **edit_kind 枚举**：plan 自创 `replace_in_block`（anchor 定位 block + 逐条 replacements）；规范要求 `replace_block / append / create_file / replace_text`（无 `replace_in_block`）
3. **action 白名单**：plan 只追加 2 个；规范要求补 6 个（`patch_app / patch_artifact / diagnose_app_bug / verify_patch / rollback_patch / promote_patch_to_generation_rule`）
4. **Preview env 注入**：plan 不涉及；规范要求从仓库根 `.env` 提白名单 13 字段注入子进程，secret redaction 全链路

本轮修复 4 条冲突，让实现与规范形态对齐。为避免继续出现“Agent 说得对但不能执行”的体验问题，Phase 1 同时纳入 5 个必要闭环能力：最小 `app_patch_targets`、可执行 fallback、patch 后 allowlist 验证、PatchSet apply 事务回滚、图片类生成默认模型对齐。完整 `AppPreviewContext`、`AdjustmentEvent`、`rollback_patch` 和 `promote_patch_to_generation_rule` 延后 Phase 2。

## 修复方案

### C1 — PatchSet 升级（单文件 → 批量事务）

**后端**：[`growth_dev/team/dashboard.py`](growth_dev/team/dashboard.py) `patch_app_generation_run` L1270 起

当前只接收单文件 `{target_path, edit_kind, anchor, new_content}`。规范要求：

```python
{
  "patches": [
    {
      "target_path": "generated_apps/<slug>/server.js",
      "edit_kind": "replace_text",
      "old_content": "const model = process.env.OPENAI_IMAGE_MODEL || \"gpt-image-1\";",
      "new_content": "const model = process.env.OPENROUTER_IMAGE_MODEL || process.env.OPENAI_IMAGE_MODEL || \"openai/gpt-5.4-image-2\";"
    }
  ],
  "preserve_capabilities": ["四阶段工作流", "产品图上传", "Prompt 下载"],
  "verification": ["node --check server.js", "node --check public/app.js"],
  "problem_source": "app_preview"
}
```

改动：

- 保留向后兼容：若 payload 含 `target_path` 单字段，自动包成 `patches=[{...}]`
- 新增 `patches:[]` 批量分支：先全部 dry-run 校验（路径、old_content 精确匹配），任一失败整体失败
- apply 事务：全部 dry-run 通过后，先生成所有新内容和 diff 到内存/临时文件；写入前备份所有目标文件；任一写入、diff 落盘或 index 更新失败时恢复所有已写目标文件，并返回 422 + 失败阶段；只有全部成功后才更新 `app_patches/index.json`
- dry_run 返回合并 diff，单次确认
- `preserve_capabilities` / `verification` / `problem_source` 作元数据写入 `app_patches/index.json`
- patch apply 成功后执行 allowlist 验证命令；v1 允许的命令仅限 app-local：`node --check server.js`、`node --check public/app.js`、`node runtime_smoke.js`、`GET /api/health`。验证失败时 patch 保留但 action result 标记 `verify_status=failed`，并阻止自动切换为“已修复”状态；若 preview 正在运行，仍按两阶段重启规则尝试启动新版本，失败保留旧进程。

**前端**：[`dashboard/app_generation.js`](dashboard/app_generation.js) `handlePatchAppAction` L1389 起

- 同时支持单文件（`action.target_path`）和批量（`action.patches`）
- 批量 diff modal 展示合并 diff（已有 unified diff 渲染可复用）
- body 透传 `patches / preserve_capabilities / verification / problem_source`

### C2 — 废弃 replace_in_block，改用 replace_text

[`growth_dev/team/dashboard.py`](growth_dev/team/dashboard.py) `_apply_edit` L1245 起

移除 plan G5 的 `replace_in_block` 分支（anchor 定位 block + 逐条 replacements），新增：

```python
if edit_kind == "replace_text":
    if not old_content:
        raise ValueError("replace_text requires old_content")
    count = original.count(old_content)
    if count == 0:
        raise ValueError(f"old_content_not_found: {old_content[:80]}")
    if count > 1:
        raise ValueError(f"ambiguous_match: old_content appears {count} times")
    return original.replace(old_content, new_content)  # 一次精确替换
```

规则：

- `old_content` 必须在文件中精确出现 **1 次**
- 0 次或 >1 次时 dry-run 失败，不写文件
- 不需要 anchor（全文匹配）

`patch_app_generation_run` 入参解析改为：单文件向后兼容读 `{target_path, edit_kind, new_content, anchor, old_content}`；批量读 `patches:[{target_path, edit_kind, old_content, new_content}]`。

### C3 — Action 白名单补齐 6 个

[`growth_dev/team/agent_bridge.py`](growth_dev/team/agent_bridge.py) L49-58 `PI_ALLOWED_ACTION_TYPES`

追加：`patch_app`, `patch_artifact`, `diagnose_app_bug`, `verify_patch`, `rollback_patch`, `promote_patch_to_generation_rule`

前端对后 4 个 action 的 stub 处理（[`dashboard/app_generation.js`](dashboard/app_generation.js) `handleAgentAction`）：

- `diagnose_app_bug`：appendAgentLog("诊断建议已收到，请根据 Agent 给出的 patch_app action 确认")
- `verify_patch` / `rollback_patch` / `promote_patch_to_generation_rule`：appendAgentLog("该动作 v1 暂未接线（action_id=...）")

避免 PI 输出这 4 个 action 时被白名单丢弃，同时不阻塞本轮主线（patch_app）。

### C4 — Preview env 注入（白名单 13 字段）

[`growth_dev/team/preview.py`](growth_dev/team/preview.py)（当前文件 grep 0 命中，需新增函数）

新增：

```python
_ENV_WHITELIST = {
    "IMAGE_PROVIDER",
    "OPENROUTER_API_KEY",
    "OPENROUTER_API_BASE_URL",
    "OPENROUTER_IMAGE_MODEL",
    "OPENROUTER_IMAGE_SIZE",
    "OPENROUTER_IMAGE_QUALITY",
    "OPENROUTER_IMAGE_OUTPUT_FORMAT",
    "IMAGE_REQUEST_TIMEOUT_MS",
    "OPENAI_API_KEY",
    "OPENAI_IMAGE_MODEL",
    "OPENAI_IMAGE_SIZE",
    "OPENAI_IMAGE_QUALITY",
    "OPENAI_IMAGE_OUTPUT_FORMAT",
}

def _inject_preview_env(repo_root: Path) -> dict[str, str]:
    """从仓库根 .env 提取白名单字段，返回注入子进程的 env dict。"""
    env_path = repo_root / ".env"
    if not env_path.exists():
        return {}
    injected = {}
    for line in env_path.read_text(encoding="utf-8").splitlines():
        stripped = line.strip()
        if not stripped or stripped.startswith("#") or "=" not in stripped:
            continue
        key, value = stripped.split("=", 1)
        key = key.strip()
        if key in _ENV_WHITELIST:
            injected[key] = value.strip().strip("'\"")
    return injected
```

`start_preview` 在 `subprocess.Popen` 前调用 `_inject_preview_env(repo_root)`，合并到 env。

[`growth_dev/team/dashboard.py`](growth_dev/team/dashboard.py) preview/start API 入参兼容：

- `inject_env=true`（新）或 `sync_env=true`（向后兼容别名），语义都是"白名单 env 注入"，不是"复制 .env 文件"
- 未来若前端还传 `sync_env`，后端按 `inject_env=true` 处理

Secret redaction 校验点（复用 [`growth_dev/team/agent_bridge.py`](growth_dev/team/agent_bridge.py) `_redact_text`）：

- preview record、logs API、SSE、Agent context、run artifact 摘要 → 不含 API key 原文
- Agent 可见：`provider/model 摘要`，例如"OpenRouter 已配置，模型 openai/gpt-5.4-image-2"，但看不到 `.env` 正文

### C5 — 图片类生成默认模型上移到 app_generation

[`growth_dev/team/app_generation.py`](growth_dev/team/app_generation.py) 与相关 prompt/scaffold/contract 逻辑：

- 图片生成类 PRD 的默认 provider/model 改为：
  - `IMAGE_PROVIDER=openrouter`
  - `OPENROUTER_IMAGE_MODEL=openai/gpt-5.4-image-2`
  - `OPENROUTER_API_BASE_URL=https://openrouter.ai/api/v1`
- 生成应用的 `.env.example` 使用占位 key，默认模型为 `openai/gpt-5.4-image-2`。
- 生成应用的服务端健康检查必须优先读取 `OPENROUTER_IMAGE_MODEL`，不得只写死 `gpt-image-1`。
- Codex/app_generation prompt 对图片类应用明确：前端模型显示/下拉来自服务端 health 或白名单，API key 只从服务端 env 读取。
- Benchmark parity 下继续保留 OpenRouter `/api/v1/images` + `input_references` 要求。

这一步解决的是“以后从 PRD 生成时默认就对”，`patch_app` 只负责已发布快照的增量修复，不再承担每次修正脚手架默认值的职责。

### G2 / G3 同步调整（PatchSet schema 教学 + intent 路由）

**G2 — Prompt 教 PatchSet schema**：[`growth_dev/team/agent_bridge.py`](growth_dev/team/agent_bridge.py) `_PI_TRAILING_ACTIONS_PROTOCOL` L60-77

重写为 4 段：

1. 通用 JSON 协议规则（保留）
2. 问题来源 → action type 路由表（从旧 plan U1 搬过来）
3. **PatchSet 新 schema**：
   ```
   {
     "type": "patch_app",
     "patches": [
       {
         "target_path": "generated_apps/<slug>/server.js",
         "edit_kind": "replace_text",
         "old_content": "逐字从 app_patch_targets[].agent_edit_anchors[] 对应锚点区间复制",
         "new_content": "修改后内容"
       }
     ],
     "preserve_capabilities": ["已通过能力清单"],
     "verification": ["node --check ..."],
     "problem_source": "app_preview / preview_error / provider_health",
     "summary": "修复摘要",
     "requires_confirmation": true
   }
   ```
4. 硬性约束：`old_content` 必须从 `AgentPromptContext.app_patch_targets[i].agent_edit_anchors[]` 指定的锚点区间逐字复制，不允许凭空生造；多次匹配或 0 次匹配时 dry-run 失败

**G3 — Intent 路由**：[`growth_dev/team/agent_bridge.py`](growth_dev/team/agent_bridge.py) `_resolve_intent` L266 起

保持原 plan 逻辑：`focus.card == "app_preview"` + 修复关键词 → `patch_app`

`_baseline_actions` 补 `patch_app` 占位 action（`source="deterministic_baseline"`），前端 L1399 对缺字段报错。

### G4 调整 — Phase 1 最小 app_patch_targets

**原 plan G4**：注入 3 文件 × 8KB 截断正文到 `app_preview.files[]`

**规范要求**：注入 `app_patch_targets[]` 摘要（path / size_bytes / content_hash / summary / agent_edit_anchors[]），不默认塞正文；PI 需要全文时通过 `read_artifact` action 拉取

**本轮调整**：不再保留 8KB 文件正文注入。Phase 1 直接实现最小 `app_patch_targets`，让 PI 有稳定锚点生成 `replace_text.old_content`，同时避免把已发布应用源码大段塞进 prompt。

[`growth_dev/team/agent_bridge.py`](growth_dev/team/agent_bridge.py) `_agent_prompt_context` L304 起：

- `focus.card == "app_preview"` 时，从 `node_context["run_dir"]` 解析 `runs/<run_id>/generated_apps/<slug>/`
- 路径安全校验：`Path.resolve()` 后必须在 `generated_apps/<slug>/` 下
- 扫描默认可 patch 文件：`server.js`、`public/index.html`、`public/app.js`、`public/styles.css`、`.env.example`、`README.md`，仅纳入存在且小于上限的文本文件
- 对每个文件输出 `path / size_bytes / content_hash / file_type / summary / agent_edit_anchors[]`
- `agent_edit_anchors[]` Phase 1 只做启发式锚点：
  - provider/model 相关行：包含 `gpt-image-1`、`OPENROUTER_IMAGE_MODEL`、`OPENAI_IMAGE_MODEL`、`IMAGE_PROVIDER`、`/api/images/generate`、`/api/health`
  - UI 操作相关行：包含 `生成`、`生图`、`download`、`model`、`provider`、`button`
  - 每个锚点包含 `anchor_id`、`line_start`、`line_end`、`purpose`、`old_content`
- `old_content` 可进入 prompt，但只限锚点片段；不得注入完整文件正文、`.env`、API key 或 iframe DOM
- 输出示例：
  ```json
  {
    "app_patch_targets": [
      {
        "path": "generated_apps/<slug>/server.js",
        "size_bytes": 3912,
        "content_hash": "sha256:...",
        "file_type": "javascript",
        "summary": "Node 本地服务，包含 health 和 image generate 路由",
        "agent_edit_anchors": [
          {
            "anchor_id": "server_model_default",
            "line_start": 42,
            "line_end": 44,
            "purpose": "图片模型默认值",
            "old_content": "const model = process.env.OPENAI_IMAGE_MODEL || \"gpt-image-1\";"
          }
        ]
      }
    ]
  }
  ```
- Phase 2 再补受控 `read_artifact` 拉全文、人工锚点和更细粒度 AST/DOM 锚点。

[`growth_dev/team/dashboard.py`](growth_dev/team/dashboard.py) 触发 agent_bridge 的地方透传 `run_dir`（复用 L1049 已有 `published_app_dir` 解析）。

### Provider text fallback（新增）

[`growth_dev/team/agent_bridge.py`](growth_dev/team/agent_bridge.py) `_parse_trailing_actions` 之后，send_message 返回前插一段：

```python
if not parsed_actions and resolved_intent == "patch_app":
    # PI 只给自然语言且方向正确，Bridge 尝试基于 app_patch_targets 生成真实 PatchSet
    fallback_action = build_patch_app_fallback(
        user_message=message,
        provider_text=provider_text,
        app_patch_targets=prompt_context.get("app_patch_targets", []),
    )
    if fallback_action:
        parsed_actions = [fallback_action]
    else:
        parsed_actions = [{
            "type": "diagnose_app_bug",
            "summary": "Provider 给出修复方向，但当前上下文无法安全定位目标文件和 old_content，请补充目标文件或打开相关预览。",
            "source": "provider_text_fallback",
            "requires_confirmation": False,
        }]
```

fallback 规则：

- 禁止生成空 `patches: []` 的 `patch_app`。空 PatchSet 不可 dry-run、不可确认执行，会退化成“说得对但不能改”。
- 只有当 `app_patch_targets` 中存在唯一匹配锚点时，才生成真实 `patch_app`。例如用户或 Provider 文本同时出现 `gpt-image-1` 与 `gpt-5.4-image-2`，且 `server.js` 锚点 `old_content` 含 `gpt-image-1`，可生成：
  ```json
  {
    "type": "patch_app",
    "source": "provider_text_fallback",
    "problem_source": "preview_error",
    "patches": [
      {
        "target_path": "generated_apps/<slug>/server.js",
        "edit_kind": "replace_text",
        "old_content": "const model = process.env.OPENAI_IMAGE_MODEL || \"gpt-image-1\";",
        "new_content": "const model = process.env.OPENROUTER_IMAGE_MODEL || process.env.OPENAI_IMAGE_MODEL || \"openai/gpt-5.4-image-2\";"
      }
    ],
    "preserve_capabilities": ["已通过的应用流程"],
    "verification": ["node --check server.js", "GET /api/health"],
    "requires_confirmation": true
  }
  ```
- 若匹配不到唯一锚点，返回 `diagnose_app_bug` 或澄清问题，不允许盲目写文件。

## 测试

新增/调整（覆盖 4 条冲突）：

**[`tests/test_agent_bridge.py`](tests/test_agent_bridge.py)**：

- `test_pi_protocol_includes_patchset_schema`：`_PI_TRAILING_ACTIONS_PROTOCOL` 含 `patches:[]`、`preserve_capabilities`、`verification`、`replace_text`
- `test_pi_allowlist_includes_six_actions`：白名单含 6 个 action
- `test_parse_trailing_actions_accepts_patchset`：模拟 PI 输出 PatchSet fenced JSON，通过白名单
- `test_provider_text_fallback_generates_real_patch_app`：PI 只返回自然语言 + app_patch_targets 有唯一锚点 → fallback 生成真实 PatchSet
- `test_provider_text_fallback_without_anchor_returns_diagnose`：无法定位唯一锚点时返回 `diagnose_app_bug`，不生成空 `patch_app`
- `test_resolve_intent_app_preview_patch`：app_preview focus + 修复关键词 → `patch_app`
- `test_prompt_context_injects_app_patch_targets`：不注入完整文件正文，只注入 `app_patch_targets` 与锚点 old_content 片段

**[`tests/test_dashboard.py`](tests/test_dashboard.py)**：

- `test_apply_edit_replace_text_success`：精确匹配 old_content 一次替换
- `test_apply_edit_replace_text_not_found`：old_content 不存在 → 抛 old_content_not_found
- `test_apply_edit_replace_text_ambiguous`：old_content 多次出现 → 抛 ambiguous_match
- `test_patch_app_patchset_dry_run_returns_merged_diff`：批量 patches → 合并 diff，不落盘
- `test_patch_app_patchset_apply_transaction`：批量 patches 全部成功 → 逐个写 diff + 文件 + index；任一校验失败 → 不写任何文件
- `test_patch_app_patchset_write_failure_rolls_back`：写入第 N 个文件失败时恢复已写文件，index 不追加成功记录
- `test_patch_app_runs_allowlisted_verification`：apply 成功后执行 allowlist 验证命令，记录 `verify_status`
- `test_patch_app_rejects_non_allowlisted_verification`：非 app-local 命令被拒绝或忽略并记录风险
- `test_patch_app_backward_compat_single_file`：单文件 `{target_path, edit_kind, old_content, new_content}` 向后兼容自动包成 patches

**[`tests/test_app_generation.py`](tests/test_app_generation.py) / [`tests/test_app_generation_image_scaffold.py`](tests/test_app_generation_image_scaffold.py)**：

- `test_image_prd_default_openrouter_model`：图片类 app contract / prompt / scaffold 默认 `OPENROUTER_IMAGE_MODEL=openai/gpt-5.4-image-2`
- `test_image_scaffold_does_not_hardcode_gpt_image_1_only`：生成应用不得只写死 `gpt-image-1`
- `test_image_health_reports_server_env_model`：health contract 要求服务端模型来自 env / 白名单

**[`tests/test_preview.py`](tests/test_preview.py)**（新增文件或在 test_dashboard 补）：

- `test_inject_preview_env_whitelist`：仓库根 `.env` 含 13 字段 → 注入子进程 env
- `test_inject_preview_env_filters_non_whitelist`：`.env` 含 `DATABASE_URL` → 不注入
- `test_secret_redaction_in_preview_record`：preview record 不含 `OPENROUTER_API_KEY` 原文

回归（必须全绿）：
`python -m unittest tests.test_agent_bridge tests.test_dashboard tests.test_app_preview_runner tests.test_app_generation tests.test_app_generation_image_scaffold tests.test_codex_executor tests.test_reference_index tests.test_benchmark_fix_slice`

## 端到端验证（用户实机）

dashboard 跑着，已发布图片应用 preview 已起，仓库根 `.env` 含 `OPENROUTER_IMAGE_MODEL=openai/gpt-5.4-image-2`：

1. 右侧切到 auto，焦点点 app_preview 卡片
2. 输入：「应用报 `gpt-image-1 · not configured`，把默认模型换成 `gpt-5.4-image-2`」
3. PI 返回回答 + 末尾 fenced JSON（PatchSet 或单文件）；若 PI 只返回自然语言，Bridge 基于 `app_patch_targets` 生成真实 fallback PatchSet；前端渲染"修改已发布应用"按钮
4. 点按钮 → diff modal 显示 server.js 变化（red→green）
5. 点确认 → patch_app 成功 → app_patches/index.json 新增记录 → allowlist 验证执行 → preview 两阶段重启
6. `curl localhost:<port>/api/health` 返回 `model=openai/gpt-5.4-image-2`
7. Agent log / preview record 不含 `OPENROUTER_API_KEY` 原文

## Phase 2 范围外（规范已定义但本轮不做）

5 条后续增强延后到独立 Phase：

1. **AppPreviewContext 派生**：`NodeContext.preview_status / preview_url / preview_health / provider_health / generated_app_capability_gaps` 作为独立子对象注入 prompt，不混在 files 里
2. **app_patch_targets 增强 + read_artifact**：Phase 1 只做启发式锚点；Phase 2 增加按需 `read_artifact`、人工锚点、AST/DOM 锚点和更细文件摘要
3. **AdjustmentEvent 落盘 + SSE**：`runs/<run>/adjustment_events.jsonl`，11 字段（event_id / user_message / resolved_intent / patch_set_id / ...），节点流"应用调优"事件轨道
4. **rollback 闭环**：`rollback_patch` dry-run + 用户确认 + reverse diff 证据 + 两阶段重启
5. **promote_patch_to_generation_rule 候选记录**：`promotion_candidates/*.json`，只记录不改模板

Phase 2 触发条件：AC-058 到 AC-064 需要验收时，或用户明确要求"Agent 驱动修复全流程"。

## 兼容性保障

- 单文件 `{target_path, edit_kind, old_content, new_content}` 向后兼容自动包成 `patches=[...]`
- `sync_env=true` 作 `inject_env=true` 别名
- 前端对 Phase 2 action（`diagnose_app_bug` / `verify_patch` / `rollback_patch` / `promote_patch_to_generation_rule`）stub 占位，不报错不阻塞
- 图片类脚手架默认值本轮同步到 `openai/gpt-5.4-image-2`；`patch_app` 只修复已发布快照，不作为每次生成后必需补丁

## 风险

1. **replace_text 全文匹配风险**：old_content 含空格、换行、注释差异时匹配失败 → 通过 dry-run 提前拦截，diff modal 让用户确认
2. **启发式锚点可能漏掉目标**：Phase 1 只扫描固定文件和关键词；漏掉时 fallback 返回 `diagnose_app_bug`，不生成空 patch；Phase 2 用 read_artifact/AST/DOM 锚点增强
3. **PI 可能不输出 patches 数组**：fallback 只有在唯一锚点可定位时生成真实 PatchSet；否则要求澄清
4. **批量 PatchSet 写入中途失败**：apply 事务使用备份/恢复，保证失败后目标文件回到原状态
5. **验证命令副作用风险**：只允许 app-local allowlist 命令；禁止 shell=True、网络探测和任意 bash
6. **Preview env 注入白名单遗漏**：13 字段来自规范明确列举，可扩展
